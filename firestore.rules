rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /stations/{stationId}/{document=**} {
      allow read, write: if request.auth.token.stationId == stationId;
    }
  }
}